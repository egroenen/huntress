services:
  huntress:
    build:
      context: .
      dockerfile: Containerfile
    image: huntress:local
    container_name: huntress
    restart: unless-stopped
    ports:
      - '47892:47892'
    volumes:
      - ./config:/config:ro
      - ./data:/data
